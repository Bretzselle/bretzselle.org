# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Gem dependencies (installed locally by bundler via flake)
vendor/bundle/

# Direnv
.direnv/

# Wayback Machine snapshot (source material, not part of the site)
websites/

# macOS
.DS_Store
